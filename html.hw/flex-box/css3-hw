:root {
    --nm : yellowgreen;
    --hv : cadetblue;
}

.box {
    width: 200px;
    height: 200px;
    background-color:  var(--nm);
}

.cont-flx {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    background-color: beige;
}

.box:hover {
    background-color: var(--hv);
}